import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './components/App';

const element = document.getElementById('google-fonts-render');

ReactDOM.render(<App />, element);
